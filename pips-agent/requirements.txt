# FastAPI web framework for building APIs
fastapi>=0.104.0

# ASGI server for running FastAPI
uvicorn>=0.24.0

# Pydantic for data validation (included with FastAPI but explicit for clarity)
pydantic>=2.0.0
